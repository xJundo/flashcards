# Connexion PostgreSQL.
DATABASE_URL=postgres://flashcards:dev@localhost:5433/flashcards

# Signe les cookies de session : `openssl rand -base64 32`.
# La changer déconnecte tout le monde.
BETTER_AUTH_SECRET=change-moi

# URL publique de l'app (cookies, redirections).
BETTER_AUTH_URL=http://localhost:3000
